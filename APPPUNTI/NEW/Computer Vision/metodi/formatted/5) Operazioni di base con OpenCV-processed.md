
| Metodo/Funzione | Descrizione | Parametri | Output |
|---|---|---|---|
| `myResourcePath(fname)` | Costruisce il percorso completo di un file, solleva un'eccezione se il file non esiste. | `fname` (stringa: nome del file) | Stringa: percorso completo del file |
| `cv2.imread(path, flags)` | Legge un'immagine da un file. | `path` (stringa: percorso del file), `flags` (intero: flag di lettura, es. `cv2.IMREAD_COLOR`) | Oggetto NumPy array (immagine) o `None` in caso di errore |
| `plt.imshow(image)` | Visualizza un'immagine. | `image` (oggetto NumPy array: immagine) | Visualizzazione dell'immagine (in Jupyter Notebook) |
| `cv2.cvtColor(src, code)` | Converte un'immagine da uno spazio colore ad un altro. | `src` (oggetto NumPy array: immagine sorgente), `code` (intero: codice di conversione, es. `cv2.COLOR_BGR2RGB`) | Oggetto NumPy array (immagine convertita) |
| `np.zeros((height, width, channels), dtype)` | Crea un array NumPy riempito con zeri. | `height` (intero: altezza), `width` (intero: larghezza), `channels` (intero: numero di canali), `dtype` (tipo di dato: es. `np.uint8`) | Oggetto NumPy array |
| `cv2.line(img, pt1, pt2, color, thickness)` | Disegna una linea su un'immagine. | `img` (oggetto NumPy array: immagine), `pt1` (tupla: punto iniziale), `pt2` (tupla: punto finale), `color` (tupla: colore BGR), `thickness` (intero: spessore) | Modifica l'immagine `img` in place |
| `cv2.rectangle(img, pt1, pt2, color, thickness)` | Disegna un rettangolo su un'immagine. | `img` (oggetto NumPy array: immagine), `pt1` (tupla: angolo superiore sinistro), `pt2` (tupla: angolo inferiore destro), `color` (tupla: colore BGR), `thickness` (intero: spessore) | Modifica l'immagine `img` in place |
| `cv2.circle(img, center, radius, color, thickness)` | Disegna un cerchio su un'immagine. | `img` (oggetto NumPy array: immagine), `center` (tupla: centro del cerchio), `radius` (intero: raggio), `color` (tupla: colore BGR), `thickness` (intero: spessore) | Modifica l'immagine `img` in place |
| `cv2.ellipse(img, center, axes, angle, startAngle, endAngle, color, thickness)` | Disegna un'ellisse su un'immagine. | `img` (oggetto NumPy array: immagine), `center` (tupla: centro dell'ellisse), `axes` (tupla: semiassi maggiore e minore), `angle` (intero: angolo di rotazione), `startAngle` (intero: angolo di inizio), `endAngle` (intero: angolo di fine), `color` (tupla: colore BGR), `thickness` (intero: spessore) | Modifica l'immagine `img` in place |
| `cv2.polylines(img, pts, isClosed, color, thickness)` | Disegna una linea poligonale su un'immagine. | `img` (oggetto NumPy array: immagine), `pts` (lista di array NumPy: punti della poligonale), `isClosed` (booleano: poligonale chiusa o aperta), `color` (tupla: colore BGR), `thickness` (intero: spessore) | Modifica l'immagine `img` in place |
| `cv2.putText(img, text, org, fontFace, fontScale, color, thickness, lineType)` | Scrive del testo su un'immagine. | `img` (oggetto NumPy array: immagine), `text` (stringa: testo), `org` (tupla: coordinate del punto di inizio del testo), `fontFace` (intero: font), `fontScale` (float: scala del font), `color` (tupla: colore BGR), `thickness` (intero: spessore), `lineType` (intero: tipo di linea) | Modifica l'immagine `img` in place |
| `print()` | Stampa un valore a console. | Valore da stampare | Nessuno |
| `type()` | Restituisce il tipo di un oggetto. | Oggetto | Tipo dell'oggetto |
| `img.shape` | Restituisce le dimensioni di un array NumPy. | Oggetto NumPy array | Tupla contenente le dimensioni dell'array |
| `img.dtype` | Restituisce il tipo di dato di un array NumPy. | Oggetto NumPy array | Tipo di dato dell'array |
| `img[50, 135]` | Accesso al valore di un singolo pixel. | Coordinate del pixel (riga, colonna) | Array NumPy rappresentante il colore del pixel (BGR) |
| `img[50+i, 135+j] = (0, 255, 0)` | Modifica il valore di un singolo pixel. | Coordinate del pixel (riga, colonna), valore del colore (BGR) | Nessun valore di ritorno esplicito, modifica l'immagine in place |
| `img[80:115, 25:60]` | Estrazione di una Region of Interest (ROI). | Coordinate di inizio e fine della ROI (riga_inizio:riga_fine, colonna_inizio:colonna_fine) | Sotto-matrice (ROI) dell'immagine originale |
| `cv2.resize(img,(2*width, 2*height), interpolation = cv2.INTER_CUBIC)` | Ridimensionamento di un'immagine. | Immagine di input, nuove dimensioni (larghezza, altezza), metodo di interpolazione | Immagine ridimensionata |
| `np.float32([[1,0,100],[0,1,50]])` | Creazione di una matrice di trasformazione (traslazione). | Valori che definiscono la trasformazione | Matrice di trasformazione NumPy |
| `cv2.warpAffine(img, M, (cols,rows))` | Applicazione di una trasformazione affine (es. traslazione). | Immagine di input, matrice di trasformazione, dimensioni dell'immagine di output | Immagine trasformata |
| `cv2.getRotationMatrix2D((cols/2,rows/2),90,1)` | Calcolo della matrice di trasformazione per la rotazione. | Centro di rotazione, angolo di rotazione, fattore di scala | Matrice di trasformazione per la rotazione |
| `cv2.getAffineTransform(pts1,pts2)` | Calcolo della matrice di trasformazione affine. | Due array NumPy con le coordinate dei punti corrispondenti nell'immagine di input e output | Matrice di trasformazione affine 2x3 |
| `cv2.getPerspectiveTransform(pts1,pts2)` | Calcolo della matrice di trasformazione prospettica. | Due array NumPy con le coordinate di quattro punti corrispondenti nell'immagine di input e output | Matrice di trasformazione prospettica 3x3 |
| `cv2.warpPerspective(img,M,(300,300))` | Applicazione di una trasformazione prospettica. | Immagine di input, matrice di trasformazione 3x3, dimensioni dell'immagine di output | Immagine trasformata |
| `cv.warpPerspective(src, M, dsize[, dst[, flags[, borderMode[, borderValue]]]])` | Applicazione di una trasformazione prospettica (versione più completa). | Immagine di input (`src`), matrice di trasformazione (`M`), dimensioni dell'immagine di output (`dsize`), immagine di output opzionale (`dst`), flag, modalità di bordo, valore di bordo | Immagine trasformata (`dst`) |
| `cv2.getPerspectiveTransform(pts1, pts2)`                                                    | Calcola la matrice di trasformazione prospettica.                                   | `pts1`: Punti sorgente; `pts2`: Punti di destinazione.                                                                                                                                              | Matrice di trasformazione 3x3.                 |
| `cv2.warpPerspective(logo, M, (cols1, rows1), imgResult, borderMode=cv2.BORDER_TRANSPARENT)` | Applica una trasformazione prospettica ad un'immagine.                              | `logo`: Immagine sorgente; `M`: Matrice di trasformazione; `(cols1, rows1)`: Dimensioni dell'immagine di output; `imgResult`: Immagine di destinazione; `borderMode`: Metodo di gestione dei bordi. | Immagine trasformata.                          |
| `cv2.calcHist([chan], [0], None, [256], [0, 256])`                                           | Calcola l'istogramma di un canale di un'immagine.                                   | `[chan]`: Canale dell'immagine; `[0]`: Indice del canale; `None`: Maschera; `[256]`: Numero di bin; `[0, 256]`: Range di valori.                                                                    | Istogramma (array di frequenze).               |
| `plt.plot(hist, color = color)`                                                              | Traccia un grafico.                                                                 | `hist`: Dati da tracciare; `color`: Colore della linea.                                                                                                                                             | Grafico.                                       |
| `plt.xlim([0, 256])`                                                                         | Imposta i limiti dell'asse x del grafico.                                           | `[0, 256]`: Limiti dell'asse x.                                                                                                                                                                     | Nessuno (modifica il grafico in-place).        |
| `np.array(features).flatten().shape`                                                         | Restituisce le dimensioni di un array appiattito.                                   | `np.array(features)`: Array di input.                                                                                                                                                               | Tupla contenente le dimensioni dell'array.     |
| `cv2.imread(myResourcePath('car.jpg'), cv2.IMREAD_COLOR)`                                    | Legge un'immagine da file.                                                          | `myResourcePath('car.jpg')`: Percorso dell'immagine; `cv2.IMREAD_COLOR`: Flag per la lettura a colori.                                                                                              | Immagine.                                      |
| `cv2.cvtColor(img,cv2.COLOR_BGR2HSV)`                                                        | Converte un'immagine da uno spazio colore ad un altro.                              | `img`: Immagine di input; `cv2.COLOR_BGR2HSV`: Flag per la conversione BGR->HSV.                                                                                                                    | Immagine convertita.                           |
| `plt.hist(np.ndarray.flatten(hue), bins=180)`                                                | Crea un istogramma.                                                                 | `np.ndarray.flatten(hue)`: Dati da tracciare; `bins`: Numero di bin.                                                                                                                                | Istogramma.                                    |
| `plt.subplot(311); plt.title("Hue")`                                                         | Crea un subplot e imposta il titolo.                                                | `311`: Posizione del subplot; `"Hue"`: Titolo del subplot.                                                                                                                                          | Nessuno (modifica il grafico in-place).        |
| `cv2.GaussianBlur(img,(5,5),0)`                                                              | Applica un filtro Gaussian Blur.                                                    | `img`: Immagine di input; `(5,5)`: Dimensione del kernel; `0`: Deviazione standard.                                                                                                                 | Immagine sfocata.                              |
| `cv2.cvtColor(blur, cv2.COLOR_BGR2RGB)`                                                      | Converte un'immagine da BGR a RGB.                                                  | `blur`: Immagine di input.                                                                                                                                                                          | Immagine convertita.                           |
| `cv2.filter2D(img, -1, kernel)`                                                              | Applica un filtro 2D all'immagine.                                                  | `img`: Immagine di input; `-1`: Tipo di dato di output; `kernel`: Kernel del filtro.                                                                                                                | Immagine filtrata.                             |
| `cv2.threshold(img,127,255,cv2.THRESH_BINARY)`                                               | Applica una soglia all'immagine.                                                    | `img`: Immagine di input; `127`: Soglia; `255`: Valore massimo; `cv2.THRESH_BINARY`: Tipo di soglia.                                                                                                | Immagine sogliata, valore di ritorno.          |
| `cv2.add(x,y)`                                                                               | Addizione di due array NumPy.                                                       | `x`, `y`: Array NumPy.                                                                                                                                                                              | Array NumPy risultante.                        |
| `x+y`                                                                                        | Addizione di due array NumPy (operatore +).                                         | `x`, `y`: Array NumPy.                                                                                                                                                                              | Array NumPy risultante.                        |
| `plt.imshow()`                                                                               | Mostra un'immagine.                                                                 | Immagine da visualizzare.                                                                                                                                                                           | Nessuno (visualizza l'immagine).               |
| `plt.hist()`                                                                                 | Crea un istogramma.                                                                 | Dati, numero di bin, etc.                                                                                                                                                                           | Istogramma.                                    |
| `plt.title()`                                                                                | Imposta il titolo di un grafico.                                                    | Titolo del grafico.                                                                                                                                                                                 | Nessuno (modifica il grafico in-place).        |
| `plt.figure()`                                                                               | Crea una nuova figura.                                                              | Dimensioni della figura.                                                                                                                                                                            | Oggetto figura.                                |
| `plt.show()`                                                                                 | Mostra la figura.                                                                   | Nessun parametro.                                                                                                                                                                                   | Nessuno (visualizza la figura).                |
| `np.ndarray.flatten()`                                                                       | Appiattisce un array NumPy in un array 1D.                                          | Array NumPy.                                                                                                                                                                                        | Array NumPy appiattito.                        |
| `cv2.resize(img, (W, H))`                                                                    | Ridimensiona l'immagine `img` alle dimensioni W x H.                                | `img`: immagine di input; `(W, H)`: tupla contenente la larghezza e l'altezza desiderate.                                                                                                           | Immagine ridimensionata.                       |
| `cv2.addWeighted(img1, alpha, img2, beta, gamma)`                                            | Esegue una combinazione lineare pesata di due immagini.                             | `img1`: prima immagine; `alpha`: peso della prima immagine; `img2`: seconda immagine; `beta`: peso della seconda immagine; `gamma`: valore costante da aggiungere.                                  | Immagine risultante dalla combinazione pesata. |
| `cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)`                                                      | Converte un'immagine dal formato BGR a scala di grigi.                              | `img`: immagine di input; `cv2.COLOR_BGR2GRAY`: codice costante per la conversione.                                                                                                                 | Immagine in scala di grigi.                    |
| `cv2.threshold(img, thresh, maxval, type)`                                                   | Applica una soglia all'immagine.                                                    | `img`: immagine di input; `thresh`: valore di soglia; `maxval`: valore massimo; `type`: tipo di soglia (es. `cv2.THRESH_BINARY`).                                                                   | Immagine binarizzata.                          |
| `cv2.bitwise_not(img)`                                                                       | Calcola l'inverso bit a bit di un'immagine.                                         | `img`: immagine di input.                                                                                                                                                                           | Immagine con i bit invertiti.                  |
| `cv2.bitwise_and(img1, img2, mask)`                                                          | Esegue un'operazione AND bit a bit tra due immagini, usando una maschera opzionale. | `img1`: prima immagine; `img2`: seconda immagine; `mask`: maschera (opzionale).                                                                                                                     | Immagine risultante dall'operazione AND.       |
| `cv2.add(img1, img2)`                                                                        | Somma due immagini.                                                                 | `img1`: prima immagine; `img2`: seconda immagine.                                                                                                                                                   | Immagine risultante dalla somma.               |

**Nota:**  `os.path.join()`, `os.path.exists()` e `RuntimeError` sono elementi del linguaggio Python standard e non specifici di OpenCV o Matplotlib.  Sono inclusi per completezza.  `%matplotlib inline` è un comando magico di Jupyter Notebook, non una funzione in senso stretto.